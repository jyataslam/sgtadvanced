const creds = {
    host     : 'host',
    user     : 'user',
    password : 'password',
    database : 'database',
    port : port
  }

  //node keywords. must use module.exports equal to the sensitive info
  module.exports = creds;